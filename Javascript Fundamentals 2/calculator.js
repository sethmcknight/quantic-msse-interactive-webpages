/*
function drink () {
    alert("Cortado");
};

drink();
*/


function add(x, y) {
    return x + y;
}

//add(3, 5);

let num1 = parseInt(prompt('Type in a number.'));
let num2 = parseInt(prompt('Type in another number.'));

alert(add(num1, num2));